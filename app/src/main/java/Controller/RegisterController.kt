package Controller

import Entity.Register
import android.widget.Toast
import com.example.proyectopersonal.RegisterActivity

class RegisterController(activity: RegisterActivity) {
    fun addRegister(register: Register) {
    }
}
